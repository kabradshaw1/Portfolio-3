import React, { useState } from 'react';
import { Navbar, Nav, Conainer, Modal, Tab} from 'react-bootstrap';

const AppNavbar = () => {
  const [showModal, setMshowModal] = useState(false);

  return (
    <>
      <Navbar bg='dark' variant='dark' expand='lg'>
        
      </Navbar>
    </>
  )
}